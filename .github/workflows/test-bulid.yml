name: bulid test run potofpolio
on:
 workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: code-action
        uses: actions/checkout@v4

      - name: buliding
        working-directory : ./potofolio
        run: |
             ls -la
             test -f index.html
             test -f photo.jpg